<!DOCTYPE html>
<html>
<head>
  <title>NewspaperApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <style>
    body { background: #EDE9D8; }
    .container { background: #EDE9D8; }
    .hero-unit { background: #FFF; }
  </style>
</head>
<body>

  <header class="navbar navbar-top navbar-inverse">
    <div class="navbar-inner">
      <ul class="nav">
        <li><%= link_to "Home", root_url %></li>
        <li><%= link_to "Create New Newspaper", new_newspaper_url %></li>
        <li><%= link_to "New user", new_user_url %></li>
      </ul>
    </div>
  </header>

  <div class="container">
    <%= yield %>
  </div>

</body>
</html>
